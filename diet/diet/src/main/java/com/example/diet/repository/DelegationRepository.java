package com.example.diet.repository;

import com.example.diet.model.Delegarion;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DelegationRepository extends JpaRepository<Delegarion, Integer> {
}
